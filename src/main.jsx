import React from "react";
import ReactDOM from "react-dom/client";
import SnackCorner from "./snack-corner.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <SnackCorner />
  </React.StrictMode>
);
